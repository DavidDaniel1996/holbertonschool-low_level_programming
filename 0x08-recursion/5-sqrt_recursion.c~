#include "main.h"

int _sqrt_recursion(int n)
{
	int r = 11;
	r = r - 2;

	if (n == 1)
	{
		return (1);
	}
/*	else if (r * r == n)
	{
		return (r);
	}*/
	else 
	{
		return (n = _sqrt_recursion(n - r));
	}
}
