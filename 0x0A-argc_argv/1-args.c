#include <stdio.h>
#include "main.h"

int main(int argc, char *argv[])
{
	int count = 0;

	if (argv[count] < argv[argc])
	{
		printf("%d\n", (argc - 1));
	}
	else
	{
		printf("%d\n", (argc - 1));
	}
	return (0);
}
